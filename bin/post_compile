#!/bin/bash

export DJANGO_SETTINGS_MODULE=src.settings

echo "Starting post_compile script..."

ls -la /
ls -la /kebab_app

python /kebab_app/manage.py compilemessages

echo "Finished compilemessages step."